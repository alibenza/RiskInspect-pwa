import React, { useState } from 'react';import { useInspectionStore } from '../hooks/useInspectionStore';import { PlusCircle, Trash2, MessageSquare, ChevronRight, ChevronLeft } from 'lucide-react';
function InspectionForm() {const { questionsConfig, responses, setResponse, addQuestion, removeSection, addSection } = useInspectionStore();const [currentStep, setCurrentStep] = useState(0);const section = questionsConfig[currentStep] || questionsConfig[0];if (!section) return <div className="p-10 text-center">Aucun volet.</div>;const answeredCount = section.questions.filter(q => responses[q.id]?.value).length;const progressPercent = section.questions.length > 0 ? Math.round((answeredCount / section.questions.length) * 100) : 0;const handleNext = () => setCurrentStep(p => Math.min(p + 1, questionsConfig.length - 1));const handlePrev = () => setCurrentStep(p => Math.max(p - 1, 0));
const renderField = (q) => {const resp = responses[q.id] || { value: '', comment: '', score: 0, isScored: false };return (<div className="space-y-3 bg-white p-2 rounded-xl"><div className="flex justify-between items-center"><label className="text-sm font-bold text-slate-700 flex items-center pr-4"><span className="w-2 h-2 bg-blue-500 rounded-full mr-2 shrink-0" />{q.label}</label><div className="flex items-center space-x-2 shrink-0"><span className="text-[10px] font-bold text-slate-400 uppercase">{resp.isScored ? 'Noté' : 'Non Noté'}</span><button onClick={() => setResponse(q.id, 'isScored', !resp.isScored)} className={`relative inline-flex h-5 w-9 items-center rounded-full transition-colors ${resp.isScored ? 'bg-blue-500' : 'bg-slate-200'}`}><span className={`inline-block h-3 w-3 transform rounded-full bg-white transition-transform shadow-sm ${resp.isScored ? 'translate-x-5' : 'translate-x-1'}`} /></button></div></div><div className="w-full">{q.type === 'number' ? (<input type="number" value={resp.value || ''} onChange={(e) => setResponse(q.id, 'value', e.target.value)} className="w-full p-3 rounded-xl bg-slate-50 border border-slate-100 text-sm font-bold focus:ring-2 focus:ring-blue-500 outline-none" placeholder="Saisir la donnée..."/>) : (<textarea rows="2" value={resp.value || ''} onChange={(e) => setResponse(q.id, 'value', e.target.value)} className="w-full p-3 rounded-xl bg-slate-50 border border-slate-100 text-sm focus:ring-2 focus:ring-blue-500 outline-none" placeholder="Informations techniques..."/>)}</div>{resp.isScored && (<div className="bg-blue-50/50 p-4 rounded-xl border border-blue-100 animate-in slide-in-from-top-2"><div className="flex justify-between text-[10px] font-bold text-blue-400 uppercase tracking-widest mb-2"><span>Critique (0)</span><span>Excellent (5)</span></div><input type="range" min="0" max="5" step="1" value={resp.score || 0} onChange={(e) => setResponse(q.id, 'score', e.target.value)} className="w-full h-2 bg-blue-200 rounded-lg appearance-none cursor-pointer accent-blue-600"/><div className="text-center font-black text-blue-600 text-lg mt-2">{resp.score || 0} / 5</div></div>)}<div className="relative"><div className="absolute left-3 top-3 text-slate-300"><MessageSquare size={14} /></div><textarea rows="2" value={resp.comment || ''} onChange={(e) => setResponse(q.id, 'comment', e.target.value)} className="w-full pl-9 pr-4 py-2 rounded-xl bg-white border border-slate-200 text-xs italic text-slate-600 outline-none" placeholder="Observations et remarques..."/></div></div>)};
return (<div className="space-y-6 pb-32 animate-in fade-in duration-300"><div className="bg-white p-4 rounded-3xl shadow-sm border border-slate-100 flex items-center justify-between"><div className="flex-1 pr-4"><p className="text-xs font-bold text-slate-400 uppercase mb-1">Volet {currentStep + 1} sur {questionsConfig.length}</p><div className="w-full bg-slate-100 rounded-full h-2"><div className="bg-blue-500 h-2 rounded-full transition-all duration-500" style={{ width: `${progressPercent}%` }}></div></div></div><span className="text-xl font-black text-blue-600">{progressPercent}%</span></div><div className="bg-white rounded-[2.5rem] p-6 shadow-md border border-slate-100 relative"><div className="flex justify-between items-start mb-8 border-b border-slate-50 pb-4"><h2 className="text-xl font-black text-slate-800 leading-tight pr-8">{section.title}</h2><button onClick={() => { if(window.confirm("Supprimer?")) { removeSection(section.id); setCurrentStep(0); } }} className="text-slate-200 hover:text-red-500 p-2"><Trash2 size={18} /></button></div><div className="space-y-8">{section.questions.map((q) => (<div key={q.id} className="animate-in slide-in-from-bottom-2 duration-300">{renderField(q)}</div>))}</div><button onClick={() => { const label = prompt("Nom du point?"); if (label) addQuestion(section.id, label, 'text') }} className="mt-10 w-full py-4 border-2 border-dashed border-slate-100 rounded-2xl text-slate-400 text-[11px] font-black uppercase flex items-center justify-center space-x-2"><PlusCircle size={16} /><span>Ajouter une question ici</span></button></div><div className="flex space-x-4"><button onClick={handlePrev} disabled={currentStep === 0} className={`flex-1 py-4 rounded-2xl font-bold flex items-center justify-center space-x-2 ${currentStep === 0 ? 'bg-slate-100 text-slate-300' : 'bg-white text-slate-700 shadow-sm border border-slate-200 active:scale-95'}`}><ChevronLeft size={20} /><span>Précédent</span></button><button onClick={handleNext} disabled={currentStep === questionsConfig.length - 1} className={`flex-1 py-4 rounded-2xl font-bold flex items-center justify-center space-x-2 ${currentStep === questionsConfig.length - 1 ? 'bg-slate-100 text-slate-300' : 'bg-blue-600 text-white shadow-md active:scale-95'}`}><span>Suivant</span><ChevronRight size={20} /></button></div><div className="fixed bottom-6 left-1/2 -translate-x-1/2 w-full max-w-md px-6 z-50"><button onClick={() => { const title = prompt("Nouveau volet ?"); if (title) { addSection(title); setCurrentStep(questionsConfig.length); } }} className="w-full bg-slate-900 text-white py-4 rounded-2xl shadow-2xl flex items-center justify-center space-x-3 border-2 border-slate-800 active:scale-95 transition-transform"><PlusCircle size={20} className="text-blue-400" /><span className="font-bold text-sm uppercase">Créer un volet</span></button></div></div>)}
export default InspectionForm;
